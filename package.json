{
  "description": "Create a static context for use with React.",
  "dependencies": {
    "react": "*"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "@babel/preset-react": "^7.8.3",
    "babel-loader": "^8.0.6",
    "babel-preset-next": "^1.2.0",
    "webpack": "^3.0.0"
  },
  "files": "bundle.js",
  "license": "ISC",
  "main": "bundle.js",
  "name": "react-static-context",
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production webpack -p --progress --colors --display-modules --sort-modules-by size"
  },
  "version": "1.5.1"
}
